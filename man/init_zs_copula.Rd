% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation_tests.R
\name{init_zs_copula}
\alias{init_zs_copula}
\title{Initialize Latent Variables for Copula Model}
\usage{
init_zs_copula(x, var_types, init_sd = 10)
}
\arguments{
\item{x}{Observed data matrix (n x p)}

\item{var_types}{Character vector specifying variable types:
"ordinal" or "binary" for each column (length p)}

\item{init_sd}{Standard deviation for initialization normal distribution (default = 10)}
}
\value{
Matrix of initialized latent variables (n x p)
}
\description{
Initializes latent variables (zs) for a copula model with mixed variable types.
For ordinal variables, maintains rank ordering through truncation bounds.
For binary variables, uses simple truncation at 0.
}
\details{
For ordinal variables:
\itemize{
\item Maintains observed rank ordering through truncation bounds
\item Uses a large initialization variance (init_sd) to allow flexibility
For binary variables:
\item Values initialized as N(0, init_sd) truncated below/above 0 depending on observed value
}
}
\examples{
# Binary data only
bin_data <- matrix(rbinom(100, 1, 0.5), ncol = 5)
zs <- init_zs_copula(bin_data, rep("binary", 5))

# Mixed ordinal/binary
mixed_data <- cbind(sample(1:5, 100, replace = TRUE),
                   matrix(rbinom(400, 1, 0.5), ncol = 4))
zs <- init_zs_copula(mixed_data, c("ordinal", rep("binary", 4)))
}
